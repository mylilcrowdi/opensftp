-r requirements.txt
pytest>=8.0
pytest-timeout>=2.3
# PySide6 6.11.0 introduced a segfault in Qt widget teardown during tests
# (gc.collect()+processEvents() after deleteLater() corrupts the C++ object).
# Pin to <6.11 until upstream fixes the issue.
PySide6<6.11
